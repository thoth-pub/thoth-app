import SpeedDialAction, { SpeedDialActionProps } from '@mui/material/SpeedDialAction';

const SpeedDialActions = (props: SpeedDialActionProps) => {
  return <SpeedDialAction {...props} />;
};

export default SpeedDialActions;
