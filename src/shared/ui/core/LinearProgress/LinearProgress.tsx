import LinearProgress, { type LinearProgressProps } from '@mui/material/LinearProgress';

const LinearProgressComponent = (props: LinearProgressProps) => {
  return <LinearProgress {...props} />;
};

export default LinearProgressComponent;
