import TableFooter, { type TableFooterProps } from '@mui/material/TableFooter';

const TableFooterComponent = (props: TableFooterProps) => {
  return <TableFooter {...props} />;
};

export default TableFooterComponent;
