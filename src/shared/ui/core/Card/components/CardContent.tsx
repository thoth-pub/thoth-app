import CardContent, { type CardContentProps } from '@mui/material/CardContent';

const CardContentComponent = (props: CardContentProps) => {
  return <CardContent {...props} />;
};

export default CardContentComponent;
