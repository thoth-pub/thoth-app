import FormHelperText, { type FormHelperTextProps } from '@mui/material/FormHelperText';

const FormHelperTextComponent = ({ ...props }: FormHelperTextProps) => {
  return <FormHelperText {...props} />;
};

export default FormHelperTextComponent;
