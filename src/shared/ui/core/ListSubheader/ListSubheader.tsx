import ListSubheader, { type ListSubheaderProps } from '@mui/material/ListSubheader';

export type ListSubheaderComponentProps = ListSubheaderProps;

const ListSubheaderComponent = (props: ListSubheaderComponentProps) => {
  return <ListSubheader {...props} />;
};

export default ListSubheaderComponent;
