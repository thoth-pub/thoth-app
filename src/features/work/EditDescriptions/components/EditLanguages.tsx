import { type BaseRecommendedSectionProps } from '@/src/shared';

export const EditLanguages = (props: BaseRecommendedSectionProps) => {
  const { workId, queryToken, recommended = false } = props;

  return <div>EditLanguages</div>;
};
