const CreateWorkForm = () => {
  return (
    <form className="flex flex-col rounded-md border-2 border-[var(--color-border-alt)] bg-[var(--color-background-alt)] p-[var(--default-content-padding)]">
      CreateWorkForm
    </form>
  );
};

export default CreateWorkForm;
